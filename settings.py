# settings.py

# Screen dimensions
SCREEN_WIDTH = 800
SCREEN_HEIGHT = 600

# Colors (R, G, B)
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
BLUE = (0, 128, 255)

# Frames per second
FPS = 60

# Variables for scrolling
scroll_x = 0  # Tracks the x-coordinate of the background
scroll_speed = 2  # Pixels per frame